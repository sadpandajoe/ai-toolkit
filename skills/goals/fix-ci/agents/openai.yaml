interface:
  display_name: "Fix CI"
  short_description: "Diagnose and fix a failing CI run, trivial, standard, and complex paths"
  default_prompt: "Use $fix-ci to diagnose and fix this CI failure."
